{
  "name": "hiremind-automation",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright TypeScript automation framework for Hiremind.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "typescript": "^5.9.3"
  }
}
