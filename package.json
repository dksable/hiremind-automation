{
  "name": "hiremind-automation",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright automation repository for AI QA Copilot validation.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "latest",
    "typescript": "latest"
  }
}
